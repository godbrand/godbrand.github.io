(function(){function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s}return e})()({1:[function(require,module,exports){
'use strict';

//const's DOM element's

var headerImg = document.querySelector('.image__header');
var section = document.querySelector('.header');
var centerHeader = document.querySelector('.center');
var aboutUs = document.querySelector('.about_us');
var aboutImage = document.querySelector('.center-box__image_about_us');
var infoAboutUs = document.querySelector('.center-box__info_about_us');
var contsctUs = document.querySelector('.contact_us');
var portfolio = document.querySelector('.portfolio');
var menu = document.querySelector('.manu__header');
var butShow = document.querySelectorAll('.show');
var showWork = document.querySelector('.show-work ');
var closeModal = document.querySelector('.close-modal');
var imgPrev = document.querySelector('#imgPrev');
var browserF = document.querySelector('#browser_first');
var browser_green = document.querySelector('#browser_green');
var browserS = document.querySelector('#browser_second');


//All functions
function showPortfolio(arrayClass) {
    for (var i = 0; i < arrayClass.length; i++) {
        arrayClass[i].addEventListener('click', function (e) {
            var modal = document.querySelector('.show-work__img-modal');
            var elem = e.target;
            var getImgSrc = elem.getAttribute('href');
            var workImgModal = document.querySelector('.show-work__img-modal');

            showWork.style.display = 'block';
            console.log(getImgSrc);

            imgPrev.setAttribute('src', getImgSrc);
            workImgModal.style.marginTop = 1000 + 'px';
            workImgModal.style.width = '100%';
            imgPrev.style.height = 'unset';

            if (getImgSrc == 'assets/img/portfolio/1.jpg') {
                workImgModal.style.marginTop = 150 + 'px';
                console.log('ads');
            }
        });
    }

    closeModal.addEventListener('click', function () {
        showWork.style.display = 'none';
    });
}

section.addEventListener('click', function (e) {

    // const's get 'TOP'
    var aboutUsTop = aboutUs.getBoundingClientRect().top;
    var portfolioTop = portfolio.getBoundingClientRect().top;
    var contactUsTop = contsctUs.getBoundingClientRect().top;

    var TargetElement = e.target.getAttribute('data-url');
    if (TargetElement == 'about') {
        window.scrollTo(0, aboutUsTop);
    }
    if (TargetElement == 'portfolio') {
        window.scrollTo(0, portfolioTop);
    }
    if (TargetElement == 'contact') {
        window.scrollTo(0, contactUsTop - 200);
    }
});

function innerDevIcon() {
    var dev = document.querySelector('.dev');
    dev.innerText = '</>';
}

function paralaxElement(container, element) {
    container.addEventListener('mousemove', function (e) {
        var clientX = e.clientX * 1 / 20;
        var clientY = e.clientY * 1 / 20;

        element.style.top = -clientY + 'px';
        element.style.left = -clientX + 'px';
    });
}

function paralaxHeader(){
    section.addEventListener('mousemove', function (e) {
        var clientX = e.clientX * 1 / 25;
        var clientY = e.clientY * 1 / 25;

        

        headerImg.style.top = -clientY + 'px';
        headerImg.style.left = -clientX + 'px';

        // Browser
        browserF.style.top =   -5 -e.clientY * 1 / 50 + '%';
        browserF.style.left = 56 -e.clientX * 1 / 120 + '%';
        //browser_green
        browser_green.style.top =   25 -e.clientY * 1 / 85 + '%';
        browser_green.style.left = 46 -e.clientX * 1 / 150 + '%';
        //browserS
        browserS.style.top =   65 -e.clientY * 1 / 100 + '%';
        browserS.style.left = 85 -e.clientX * 1 / 180 + '%';
    });
}

function paralaxScroll(elem, secondelem) {
    window.addEventListener('scroll', function (e) {
        var scrolled = window.scrollY / 4.5;

        elem.style.marginTop = -scrolled + 'px';
        secondelem.style.marginTop = -window.scrollY / 4 + 'px';
    });
}
//

function headerContainer() {
    innerDevIcon();
    paralaxScroll(centerHeader, headerImg);
}

function aboutUsContainer() {
    paralaxScroll(infoAboutUs, aboutImage);
}

//

headerContainer();
aboutUsContainer();
showPortfolio(butShow);
paralaxHeader();
},{}]},{},[1])

//# sourceMappingURL=maps/app.js.map
